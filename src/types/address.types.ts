export type Address = {
  house_num?: 0;
  street: string;
  landmark: string;
  area: string;
  pincode: string;
  city: string;
  state: string;
  name: string;
  phone: string;
};

export type AddressOptional = Address;
