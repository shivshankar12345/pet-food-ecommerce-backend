export type SaveUserParams = {
  email: string;
};

export type UserOptional = {
  name?: string;
  email?: string;
  phone?: string;
  pan_num?: string;
  gst_num?: string;
  gender?: string;
};

export type Id = {
  id: string;
};
